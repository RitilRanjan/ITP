{
  "id": "level2",
  "name": "Predecessor using Iota",
  "guide_hints": [
    "# Level 2: Predecessor using Iota\n\nWelcome to Level 2! Here we introduce the axiom of injectivity for the successor function `S`, available as `S_injective`. It states that `S x = S y ⇒ x = y`.",
    "Your goal is to define the predecessor function `P` and prove that `∀x P S x = x`.\n\nThe theoretically correct way to define this is using the `ι` (iota) operator for definite descriptions ('THE unique y'), because otherwise `P` behaves like a multivalued function. To use `ι` effectively, we will need the `S_injective` axiom.",
    "### Instructions:\nStep 1: First, create standard variables `x, y` by typing `cv x y`.",
    "Step 2: Now define the predecessor function `P`. The predecessor of `x` is the UNIQUE `y` such that `S y = x`. Type `def_f 1 P x ι y S y = x`.",
    "Step 3: Create the goal formula and start the mission. Type `cf goal ∀x P S x = x` followed by `mission goal`.",
    "Step 4: Strip the universal quantifier from the goal using `intro u`. (This creates fresh variable `u`).",
    "Step 5: Unfold `P` by typing `fold P (1)`. Notice that your goal now contains the iota operator `ι`.",
    "Step 6: Unfold the `ι` operator by clicking on it and selecting `fold` or typing `fold ι (1)`. It will prompt you for two fresh variables; use `v` and `w` (type them in the popup input boxes or use `v w` in terminal).",
    "Step 7: The unfolded goal is `∃v (∀w (S w = S u ⇔ v = w) ∧ v = u)`. To prove an existential statement, you construct the statement for a specific witness. Here, the witness for `v` is `u`. Use `intro u`.",
    "Step 8: The goal becomes `∀w (S w = S u ⇔ u = w) ∧ u = u`. Break it into two goals by typing `and g1 g2`.",
    "Step 9: The first goal `g1` is `u = u`. Prove it using `apply E1`.",
    "Step 10: Now for the second goal `g2` which is `∀w (S w = S u ⇔ u = w)`. Type `intro z` to make it `S z = S u ⇔ u = z`.",
    "Step 11: Type `swap_eq (1)` to convert it into `S u = S z ⇔ u = z`. Then break this equivalence into two implications using the `iff` command.",
    "Step 12: You will get two implication goals. One is `u = z ⇒ S u = S z`, which you can prove using `apply E2`. The other is `S u = S z ⇒ u = z`, which you prove using `apply S_injective`. Good luck!"
  ],
  "allowed_commands": [
    "cv",
    "cf",
    "mission",
    "swap_eq",
    "apply",
    "show",
    "help",
    "def_f",
    "intro",
    "fold",
    "and",
    "iff"
  ],
  "allowed_axioms": [
    "E1",
    "E2",
    "E3",
    "P1",
    "P2",
    "P3",
    "P4",
    "S_injective"
  ],
  "allowed_rules": [
    "QR1",
    "QR2",
    "PC1",
    "PC2"
  ],
  "goal_statement": "∀x P S x = x",
  "start_env": null,
  "theory": "NT"
}